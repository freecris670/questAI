# Railway монорепозиторий конфигурация
[build]
builder = "nixpacks"
nixpacksToml = "nixpacks.toml"
watchPatterns = ["apps/**", "packages/**"]

[deploy]
numReplicas = 1
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Настройки для монорепозитория
[monorepo]
type = "pnpm"
root = "."
